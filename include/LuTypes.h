#ifndef LU_TYPES
#define LU_TYPES

#include <inttypes.h>
#include <stdbool.h>
#include <stddef.h>

typedef uint8_t byte;
typedef uint16_t LuAngle;
typedef int32_t (*CompareFunc)(const void*, const void*);

#endif	// LU_TYPES
